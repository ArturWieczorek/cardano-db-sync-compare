# Development-only dependencies: tests, linting, type-checking, pre-commit.
# Installs the runtime deps as well via -r.

-r requirements.txt

mypy==1.13.0
pre-commit==4.0.1
pytest==8.3.4
pytest-postgresql==6.1.1
ruff==0.11.7

# Transitive dep of pytest-postgresql (pinned for reproducibility).
port-for==0.7.4
